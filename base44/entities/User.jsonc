{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "agent",
        "supervisor"
      ],
      "default": "user",
      "description": "User role in the system"
    }
  }
}